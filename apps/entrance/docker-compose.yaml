name: entrance
services:
  entrance:
    image: entrance:latest
    container_name: entrance
    network_mode: host
    volumes:
      - ${ENTRANCE_CONFIG_FILE:-./config.yaml}:/app/config.yaml:ro
      - /run/avahi-daemon:/run/avahi-daemon:ro
    environment:
      - ENTRANCE_CONFIG_FILE=/app/config.yaml
    restart: unless-stopped
