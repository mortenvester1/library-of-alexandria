log_level: INFO
allowed_origins:
  - "*"
apps:
  - name: "Jupyter Notebook"
    port: 8888
    description: "Data science notebook"

  - name: "Grafana"
    port: 3000
    route: /grafana
    description: "Monitoring and visualization"

  # Use host IP instead of hostname (for services that require IP-based access)
  - name: "MiniDLNA"
    port: 8200
    use_host_ip: true
    description: "Media server requiring IP-based access"
