List all available wikis in the library.

Example: `/wiki-list`

---

List the subdirectories of `~/git/library-of-alexandria/library/` that contain a `CLAUDE.md` file — each one is a wiki. Print the wiki names, one per line.
