Health-check a wiki for contradictions, orphans, and gaps.

Arguments: `<wiki-name>`

Example: `/wiki-lint arcanum`

---

The wiki is at `~/git/library-of-alexandria/library/$ARGUMENTS`.

1. Read `~/git/library-of-alexandria/library/<wiki-name>/CLAUDE.md` for schema and conventions
2. Follow the Lint operation defined there exactly
