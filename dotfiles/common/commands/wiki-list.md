List all available wikis in the library.

Example: `/wiki-list`

---

List the subdirectories and symbolic links in `~/git/library-of-alexandria/library/` that contain a `AGENTS.md` file — each one is a wiki. Print the wiki names, one per line.
